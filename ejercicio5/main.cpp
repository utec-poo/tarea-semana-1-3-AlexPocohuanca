#include <iostream>
#include "Tipos.h"
#include <vector>
using namespace std;

int main() {

entero numero;
caracter crt;
vector<int> listaa={};
do{
  entero aux;
cout<<"Ingresa un numero";
  listaaa.push_back(aux);
}while(aux!=0);
  
  
  
  
  
  cout<<"Los numeros ingresados son: "<<listaa.size()-1<<endl;
  return 0;
}

